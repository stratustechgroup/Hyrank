# Loops.so API Key for waitlist signups
# Get your API key from: https://app.loops.so/settings?page=api
# This key is kept server-side only (never exposed to the client)
LOOPS_API_KEY=your_loops_api_key_here
