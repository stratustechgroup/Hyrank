#!/bin/sh
# ============================================================================
# HyRank Vote Plugin — Gradle wrapper bootstrap script
#
# NOTE: This is a placeholder. The real gradlew binary is generated by running:
#   gradle wrapper
# from within the plugin/ directory on a machine that has Gradle 8+ installed.
#
# To bootstrap on macOS:
#   brew install gradle
#   cd /Users/jamesfarmer/projectsv2/hyrank/Hyrank/plugin
#   gradle wrapper
#   ./gradlew clean build    # builds build/libs/hyrank-vote-plugin-0.1.0.jar
#   ./gradlew test           # runs JUnit 5 tests
#
# ============================================================================
echo "ERROR: Gradle wrapper not yet bootstrapped."
echo "Run: brew install gradle && cd plugin && gradle wrapper"
exit 1
